# 日志提取与排查实施方案

##小零售

## 一、方案概述

针对应用层、嵌入式、中间件、视觉四类日志，搭建一体化日志排查体系，实现**错误溯源提取、时段定向提取、就近时间截取**三类核心能力，快速定位故障诱因，适配单文件多错误场景，支撑问题复盘分析。

## 二、核心业务功能

### 功能 1：错误驱动式日志提取

1. 选定待核查目标日志文件，全文遍历检索错误、失败、异常、报错类关键字内容

2. 识别到单条 / 多条错误记录后，抓取每条错误对应的精准时间戳

3. 以错误时间为基准，统一截取**前后十分钟**范围数据

4. 同步调取同时间段内应用、嵌入式、中间件、视觉全品类日志，按错误次数拆分输出独立日志包

### 功能 2：自定义时间段日志提取

1. 人工录入起始、截止时间节点

2. 批量筛选四类日志文件，匹配时间区间内所有日志条目

3. 汇总整合跨类型日志，生成完整时段日志文档

### 功能 3：就近时间日志截取

1. 输入参考时间点

2. 以该时间为中心，截取预设时长范围内全部日志数据

3. 快速获取事件周边上下文日志信息

## 三、日志分类范围

1.应用层日志业务：代码运行、接口调用、程序报错、逻辑异常记录
2.嵌入式日志:底层硬件驱动、设备通信、运动控制运行日志
3.中间件日志:消息转发、数据交互、协议传输类运行记录
4.视觉日志:图像采集、识别运算、点位校准相关日志

## 四、整体作业流程

### 流程一：错误日志排查提取

1. 文件选取：确定需要筛查的应用日志源文件

2. 错误检索：扫描文件，标记所有报错、失败类日志及对应时间

3. 区间划定：每条错误分别划定前后十分钟时间范围

4. 跨库归集：按划定时间，调取四类配套日志

5. 分类归档：多条错误分别生成独立日志文件，便于逐个分析

### 流程二：指定时段提取

1. 录入起止时间参数

2. 遍历全部日志存储目录，筛选符合时间范围数据

3. 合并多类型日志，规整排序后输出完整日志文件

### 流程三：就近时间提取

1. 输入参照时间

2. 设定截取时长，自动计算前后时间边界

3. 聚合边界内所有类别日志，完成提取输出

## 五、规则规范

1. 时间基准：统一以日志自带时间戳为唯一判定依据，时间格式全局对齐

2. 多错处理：单文件存在多处错误时，每条错误独立截取日志，互不混杂

3. 日志完整性：提取过程保留原始日志格式、告警级别、报错详情，不删减有效信息

4. 存储输出：按提取模式命名归档，区分错误包、时段包、就近包，便于查阅溯源

## 六、应用场景

1. 程序崩溃、接口调用失败时，调取错误周边全链路日志定位根因

2. 复盘特定时段批量故障，统一汇总时段内所有运行数据

3. 已知故障大致时间，快速截取周边日志核查事件前后运行状态

